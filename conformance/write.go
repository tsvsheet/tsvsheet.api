// The write half of the conformance table: creating, replacing, deleting, and
// the preconditions each requires. Split from the read half only for size —
// both run against every Factory.
package conformance
